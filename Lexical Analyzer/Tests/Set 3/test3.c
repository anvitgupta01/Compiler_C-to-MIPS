#include <stdio.h>

int main() {
    /* This is an unclosed comment   // ❌ Lexical Error: Comment is not closed
    
    int héllo = 25;  // ❌ Lexical Error: Non-ASCII character in identifier (é)

    printf("Number: %d\n", héllo);
    return 0;
}
